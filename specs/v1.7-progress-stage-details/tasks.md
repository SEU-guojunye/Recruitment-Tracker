# Implementation Plan

- [x] 1. 扩展进度节点数据契约与兼容层
  - 为默认、新增及 CSV 回退节点增加 `note`。
  - 为旧本地数据与 CloudBase 快照补兼容默认值。
  - 增加节点备注类型和长度校验，并验证 CSV/快照无损往返。
  - _Requirements: R1, R5_

- [x] 2. 在编辑进度弹窗中支持节点备注
  - 为每个环节增加“备注或面试链接”多行输入框。
  - 保存时裁剪首尾空白，保留内部换行。
  - 调整桌面与手机端环节布局并补充编辑流程测试。
  - _Requirements: R2, R4, R5_

- [ ] 3. 实现招聘进度节点详情展开
  - 将节点改为可访问的展开触发器，并保持现有 Steps 状态视觉。
  - 增加单一详情面板、空态、日期和备注展示。
  - 安全识别并渲染 HTTP/HTTPS 外链。
  - _Requirements: R3, R4_

- [ ] 4. 完成响应式、兼容和回归验收
  - 覆盖旧数据、CSV、CloudBase 快照、扩展写入和 Web 只读展示。
  - 验证桌面、320–430px 手机端、键盘操作与无水平溢出。
  - 运行 lint、单元测试、构建、完整 E2E、浏览器检查和 CloudBase 代码审查。
  - _Requirements: R1-R5_
